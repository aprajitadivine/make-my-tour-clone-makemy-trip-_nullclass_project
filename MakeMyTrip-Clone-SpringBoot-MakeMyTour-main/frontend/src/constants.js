/**
 * Shared constants used across multiple modules.
 * Centralising these prevents typos and ensures consistency.
 */

// ── localStorage keys ─────────────────────────────────────────
/** Key under which the user's preferred language is persisted */
export const STORAGE_KEY_LANGUAGE = 'preferredLanguage'
/** Key under which the user's dark mode preference is persisted */
export const STORAGE_KEY_DARK_MODE = 'darkMode'
/** Key under which the JWT access token is stored */
export const STORAGE_KEY_TOKEN = 'token'
